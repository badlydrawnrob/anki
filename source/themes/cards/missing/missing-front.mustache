<!-- Cloze card ====

    - Type: A question with a [missing] word
            A `missing->answer` card;
            we're hiding one or more words to guess the answer.
    - Docs: https://github.com/badlydrawnrob/anki/tree/master/source/docs/missing/index.md
    - Key:  ★ Required
            ☆ Optional (recommended)
            ✎ Optional (notes, markdown)
            ⤷ Field Type

==== -->

<div id="front" class="anki-Front">
  <section class="missing missing-front">
    <header class="missing-Header">
      <h1 class="missing-Header_Title">
        {{★ Title}}  <!-- ⤷ `string` (automatically wrapped in `h1`) -->
      </h1>
      {{#☆ Subtitle}}
        <h2 class="missing-Header_Subtitle">
          {{☆ Subtitle}}  <!-- ⤷ `string` (automatically wrapped in `h2`)  -->
        </h2>
      {{/☆ Subtitle}}
      {{#☆ Syntax (inline code)}}
        <p class="missing-Header_Code">
          <code>{{☆ Syntax (inline code)}}</code>  <!-- ⤷ `code string` -->
        </p>                                       <!-- (automatically wrapped in `<p><code>`) -->
      {{/☆ Syntax (inline code)}}
    </header>
    <div class="missing-KeyPoint">
      <div class="missing-KeyPoint_Code">
        {{cloze:★ Key point (code block or image)}}  <!-- ⤷ `pre code | image` -->
      </div>                                         <!-- (requires `markdown->html` conversion) -->
    </div>                                           <!-- (requires `{{c1:cloze}}` tag) -->
  </section>
</div>
