<div id="reverse">
  <div id="answer">  <!-- Missing! has it's #answer tag here (wraps the whole of reverse) -->
    <section class="gl-Card">
      <header>
        <h1>{{{★ Title}}}</h1>
        {{#☆ Subtitle}}
          <h2>{{{☆ Subtitle}}}</h2>
        {{/☆ Subtitle}}
        {{#☆ Syntax (inline code)}}
          <p><code>{{{☆ Syntax (inline code)}}}</code></p>
        {{/☆ Syntax (inline code)}}
      </header>
      <div class="gl-Card_KeyPoint">
        <div class="gl-CodeBlock">
          {{{★ Key point (code block) — cloze reverse}}} <!-- special name for .json only -->
        </div>
        <div class="gl-Card_KeyPointNotes">
          {{{★ Key point notes}}}
        </div>
      </div>
      <footer>
        {{#✎ Other notes}}
          {{{✎ Other notes}}}
        {{/✎ Other notes}}
        <small>{{{Tags}}}</small>
      </footer>
    </section>
  </div>
</div>
